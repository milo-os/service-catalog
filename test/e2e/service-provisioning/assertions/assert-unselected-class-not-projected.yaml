# The class the selector did not match must not reach the consumer project.
apiVersion: networking.datumapis.com/v1alpha
kind: IPClass
metadata:
  name: networking-e2e-datumapis-com-e2e-internal-only
